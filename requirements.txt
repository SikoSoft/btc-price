# HTTP
requests>=2.31.0

# Data / analysis (used in analysis/ layer)
pandas>=2.0.0
numpy>=1.24.0

# API server (used in api/ layer — not needed until that layer is built)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
